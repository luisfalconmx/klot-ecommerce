export interface CartCardProps {
  name: string;
  availableStock: number;
  defaultQuantity: number;
  unitaryPrice: number;
  merchandiseId: string;
  image?: string;
  size?: string;
  color?: string;
}
