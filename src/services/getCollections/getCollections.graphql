query GetCollections {
  collections(first: 10, sortKey: TITLE, reverse: true) {
    edges {
      node {
        id
        handle
        title
        image {
          url
        }
      }
    }
  }
}
