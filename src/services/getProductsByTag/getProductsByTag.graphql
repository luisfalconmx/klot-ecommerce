query GetProductsByTag($tag: String!) {
  products(first: 10, query: $tag) {
    nodes {
      handle
      title
      featuredImage {
        url
      }
      priceRangeV2 {
        minVariantPrice {
          amount
        }
      }
    }
  }
}
