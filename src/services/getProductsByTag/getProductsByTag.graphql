query GetProductsByTag($tag: String!) {
  products(first: 10, query: $tag) {
    nodes {
      id
      handle
      title
      featuredImage {
        url
      }
      priceRangeV2 {
        minVariantPrice {
          amount
        }
      }
    }
  }
}
